import { CalendarPlus, CheckCircle2, Download, Home } from "lucide-react";
import { Button } from "@/components/ui/button";
import { downloadIcs, googleCalendarUrl } from "@/lib/calendar";
type Booking={id:string;booking_date:string;booking_time:string;services:string[]};
export function BookingConfirmation({booking,onHome}:{booking:Booking;onHome:()=>void}){const item={id:booking.id,date:booking.booking_date,time:booking.booking_time,services:booking.services};return <div className="min-h-[75vh] flex items-center justify-center px-4 py-16"><div className="max-w-xl w-full text-center"><CheckCircle2 className="mx-auto h-14 w-14 text-primary mb-6"/><p className="customer-kicker">RESERVA CONFIRMADA</p><h1 className="noir-title mt-3">Nos vemos <em>pronto.</em></h1><p className="noir-lede mx-auto">Tu cita está guardada. Añádela a tu calendario para tenerla siempre a mano.</p><div className="noir-glass rounded-lg p-5 mt-8 text-left"><p className="font-semibold capitalize">{new Date(`${booking.booking_date}T00:00:00`).toLocaleDateString("es-ES",{weekday:"long",day:"numeric",month:"long"})}</p><p className="text-3xl font-semibold text-primary my-2">{booking.booking_time.slice(0,5)}</p><p className="text-sm text-muted-foreground">{booking.services.join(" · ")}</p></div><div className="grid sm:grid-cols-2 gap-3 mt-6"><Button asChild variant="premium"><a href={googleCalendarUrl(item)} target="_blank" rel="noreferrer"><CalendarPlus/>Google Calendar</a></Button><Button variant="outline" onClick={()=>downloadIcs(item)}><Download/>Apple / archivo .ics</Button></div><Button variant="ghost" onClick={onHome} className="mt-5"><Home/>Volver al inicio</Button></div></div>}
